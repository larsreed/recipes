# TODO

1. on export - subrecipes are exported twice if shown in the list
2. warn if a recipe points to a non-existing source or subrecipe on import
2. make it possible to reorder recipes and subrecipes in the print view, and also different number of people for each recipe (to avoid large desserts...)
3. add dropdown filter for categories in recipe list
4. complete implem of goto subrecipe
5. Styling & layout
6. add pictures to Sources
7. ability to use visual editor in larger text fields
8. make it possible to group ingredients
9. ESC to close: check if dirty first, also for SourceModal
10. show error messages as banners, and remove after a while
11. Change to Postgresql
